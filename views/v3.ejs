<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="apple-touch-icon"
      sizes="180x180"
      href="/icon/apple-touch-icon.png"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="32x32"
      href="/icon/android-chrome-512x512.png"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="16x16"
      href="/icon/android-chrome-192x192.png"
    />
    <link rel="manifest" href="/icon/site.webmanifest" />
    <link rel="stylesheet" href="/css/v1.css" />
    <title>v3</title>
  </head>
  <body>
    <h1>Pending Student V3 verification list</h1>
    <ol>
      <% for( let x of data ) { %> <%if(!x.verification3 &&
      x.verification3Status===process.env.STATUS && x.verification1===true &&
      x.verification2===true) { %>
      <li><a href="/v3/<%=x.googleId%>"><%=x.username%></a></li>
      <%}%> <% } %>
    </ol>
  </body>
</html>
